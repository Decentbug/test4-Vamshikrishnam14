#include <stdio.h>
int main()
{
  int a,b,c;
  printf("enter 3 numbers");
  scanf("%d%d%d",&a,&b,&C);
  if((a,b)&&(a>c))
  printf("a is &p\n");
  else
   if ((b,a)&&(b>c))
  printf("b is &p\n");
  else
  printf("c is &p\n");
  return 0 ;
  }